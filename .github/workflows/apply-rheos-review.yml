name: Archived Rheos Review Migration

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived workflow
        run: |
          echo "The one-shot Rheos migration and verification are complete."
          echo "Use the Repository Contracts workflow for ongoing validation."
